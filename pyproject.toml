[project]
name = "image-processing-experiments"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aadit", email = "aadits2004@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.1",
    "opencv-python>=5.0.0.93",
]

[project.scripts]
image-processing-experiments = "image_processing_experiments:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false

